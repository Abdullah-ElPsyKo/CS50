SELECT TITLE FROM MOVIES WHERE ID IN (SELECT MOVIE_ID FROM STARS WHERE PERSON_ID IN (SELECT ID FROM PEOPLE WHERE NAME = 'Bradley Cooper' OR NAME = 'Jennifer Lawrence') GROUP BY MOVIE_ID HAVING COUNT(MOVIE_ID) = 2);
